pipeline{
        agent {
                label 'tomcat'
            }


               stages {
                        stage('checkout') {
                                steps {
                                   git credentialsId: 'ssh', url: 'https://github.com/himanaidu56/tomcat_project.git'
                                        }
                                }


                       stage('test') {
                                steps {
                                    sh 'mvn clean test'
                                        }
                                }


                       stage('build') {
                                steps {
                                    echo 'This is build Stage'
                                        }
                                }


                       stage('deploy') {
                                steps {
                                    sh 'sudo cp /home/ubuntu/jenkins/workspace/declarative/target/*.war /opt/apache-tomcat-9.0.84/webapps/'
                                        }
                                }


               }
}
